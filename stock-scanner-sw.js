'use strict';
const CACHE='stock-scanner-shell-v5-3-service-content3';
const SHELL=['./','./index.html','./root_redirect.js','./stock-scanner.html','./stock-scanner.css','./stock_scanner_runtime.js','./methodology_education_v1.json','./public_config.js','./brand.json','./stock-scanner.webmanifest','./icon.svg','./legal/terms.html','./legal/privacy.html','./status.html','./support.html','./stock-scanner-qr.png'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting()));});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>(key.startsWith('stock-scanner-')||key.startsWith('kabulens-shell-'))&&key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim()));});
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(request.mode==='navigate'){const isPolicy=/\/(legal\/[^/]+|status|support)\.html$/.test(url.pathname);const fallback=isPolicy?request:'./stock-scanner.html';event.respondWith(fetch(request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(request,copy));return response;}).catch(()=>caches.match(request).then(found=>found||caches.match(fallback))));return;}if(SHELL.some(asset=>url.pathname.endsWith(asset.replace(/^\.\//,''))))event.respondWith(fetch(request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(request,copy));return response;}).catch(()=>caches.match(request)));});
